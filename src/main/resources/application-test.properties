# spring.application.name=demo
# server.port=9001
# server.forward-headers-strategy=framework

# spring.datasource.url=jdbc:mysql://localhost:3306/transport_pro?createDatabaseIfNotExist=true
# spring.datasource.username=root
# spring.datasource.password=Amypo

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# spring.jpa.open-in-view=false

# jwt.secret=mySecretKeyThatIsLongEnoughForJWTHMACAlgorithmAndMeetsThe256BitRequirement
# jwt.expiration=86400000

spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=root
spring.datasource.password=Amypo

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# ===============================
# FORCE schema.sql EXECUTION
# ===============================
spring.sql.init.mode=always

# ===============================
# TEST SAFETY
# ===============================
server.port=0
